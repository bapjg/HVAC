Installation Details
====================

Eclipse
=======
Eclipse IDE for Java EE Developers
Add ObjectAid (UML plugIn)

TomCat
======
Download TomCat from http://tomcat.apache.org/download-80.cgi
                     ----------------------------------------
                     
Choose 32bit/64bit Windows Service installer

http://www.youtube.com/watch?v=EOkN5IPoJVs




Notes 12/10/2016
================

scanAndSet receives garbage so does not identify spi_bigrealay
probably a speed problem

Interfaces.c : scanAndSet modified to just do a return

Note that Interfaces.o and libInterfaces.so was in git, so would overwrite any new compilation.
Modified to just exit.

To rebuild libInterfaces.so do : 
	make libInterfaces.so
This recompiles Interfaces.c and builds the libInterfaces.so file

Recompile Interfaces.c
======================

make so							// This compiles to Interfaces.o
								// and linkedits to libInterfaces.so


Git
===

git pull						// Gets the current HEAD

git stash save					// Deletes the latest modifications
git stash drop					// from the repository

git add .						// Adds files to the git index
git commit -a -m "commit msg" 	// -m Any message will do. -a (or --all) all updated/deleted files but not new files (unless git add used).
git push						// pushes to the upstream
								// prompts for user/pwd = bapjg/bapjg7478

							